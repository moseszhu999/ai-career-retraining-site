# Copy to .streamlit/secrets.toml for local production-like testing.
# In Streamlit Community Cloud / other hosting, configure these as app secrets.

DATA_BACKEND = "supabase"
TENANT_CODE = "demo"
SUPABASE_URL = "https://YOUR_PROJECT.supabase.co"
SUPABASE_SERVICE_ROLE_KEY = "YOUR_SERVICE_ROLE_KEY"

# For public demos without persistence, omit this file or use:
# DATA_BACKEND = "session"
